# 컴박사 웹사이트 개선 PRD

> 문서 버전: v1.3
>
> 작성일: 2026-07-29
>
> 상태: 구현 검토 완료안
>
> 서비스명: 컴박사
>
> 운영 URL: <https://combaksa-computer-repair.jung372.workers.dev/>
>
> 대표 연락처: `010-3388-1597`

## 1. 문서 목적

현재 운영 중인 컴박사 웹사이트를 기준으로 다음 변경사항의 제품 요구사항, 보안 설계, 데이터 호환성, 구현 범위와 인수 조건을 정의한다.

1. 서비스 신청 폼에서 우편번호 입력란을 제거한다.
2. 신규 비공개 글 비밀번호를 4~20자로 설정한다.
3. 운영자가 최초 비밀번호를 안전하게 설정하고 로그인 후 웹에서 변경할 수 있게 한다.
4. 웹사이트의 대표 연락처를 `010-3388-1597`로 통일한다.
5. 홈 화면의 `빠른 확인 · 접수 즉시 알림` 카드가 존재하는 목적과 실제 기능의 관계를 명확히 한다.
6. 전체 신청 게시판을 없애고 휴대전화 번호와 비밀번호로 본인의 신청만 조회하게 한다.
7. 이름과 상세 주소를 선택 입력으로 바꾸고 이름 미입력 시 `미상`으로 저장한다.
8. 관리자 화면을 고정 일련번호 기반 접수 관리대장, 복합 검색, 상세 편집 구조로 확장한다.

이 문서는 기존 구현을 전면 교체하는 신규 제품 기획서가 아니라, 현재 vinext/Cloudflare Workers/D1 기반 제품에 적용할 변경 PRD다.

## 2. 제품 요약

컴박사는 고객이 회원가입 없이 컴퓨터 수리를 신청하고 진행 상태를 조회하며, 운영자가 관리자 화면과 Telegram 알림으로 접수를 처리하는 모바일 우선 웹사이트다.

이번 개선의 핵심은 다음과 같다.

- 고객 입력 부담과 불필요한 개인정보 수집을 줄인다.
- 비공개 글 비밀번호 정책을 사용자가 요청한 4~20자로 변경한다.
- 공개 신청 목록 대신 휴대전화 번호와 신청 비밀번호가 모두 일치하는 개인 조회 흐름을 제공한다.
- 환경변수에 평문 운영자 비밀번호를 두는 현재 구조를 DB 해시 기반 구조로 전환한다.
- 대표 연락처와 자동 알림 관련 문구가 실제 운영 상태와 일치하게 한다.

## 3. 현행 구현 검토

| 영역 | 현재 상태 | 문제 또는 영향 |
|---|---|---|
| 우편번호 | 신청 폼의 필수 입력값이며 서버 검증, D1, 관리자 상세, 개인정보 처리방침에 연결됨 | UI만 삭제하면 신청이 서버에서 거부되므로 관련 계층을 함께 변경해야 함 |
| 비공개 글 비밀번호 | 생성·조회 모두 8~64자 | 신규 정책을 단순 변경하면 기존 20자 초과 비밀번호 글의 조회가 차단될 수 있음 |
| 운영자 비밀번호 | `ADMIN_PASSWORD` 런타임 값과 평문 비교 | 값이 없으면 운영 로그인 불가, 웹 최초 설정·변경·세션 일괄 만료 기능 없음 |
| 비밀번호 저장 | 비공개 글은 Argon2id 해시 사용 | 동일 해시 모듈을 운영자 비밀번호에도 재사용 가능 |
| 운영자 세션 | 8시간 유효한 HMAC 서명 쿠키 | 비밀번호 변경 후 기존 세션을 서버에서 즉시 만료시키기 어려움 |
| 대표 연락처 | 기본값 `010-0000-0000`, 런타임 설정이 있으면 우선 | 실제 연락처와 불일치 |
| 즉시 알림 카드 | 홈 히어로의 정적 플로팅 카드 | 실제 알림 성공 상태와 연결되지 않아 운영 설정이 없을 때 오해 가능 |
| 신규 접수 알림 | 접수 저장과 함께 outbox 생성 후 Telegram 전송 시도 | 알림 실패와 신청 저장은 분리되어 있어 신청 유실은 방지됨 |
| 신청 현황 | 홈과 `/requests`에서 전체 신청 목록을 노출 | 접수량이 적으면 낮은 활성도를 강조하고 개인정보 노출 표면만 넓어질 수 있음 |
| 공개 범위 | 고객이 `PUBLIC` 또는 `PRIVATE`를 선택 | 개인 조회 방식에서는 신규 접수를 모두 비공개로 단순화하는 편이 일관됨 |

## 4. 목표와 비목표

### 4.1 목표

1. 고객이 우편번호 없이 기본 주소만으로 신청을 완료하며 이름과 상세 주소는 선택 입력한다.
2. 신규 비공개 글은 4~20자 비밀번호를 사용한다.
3. 기존 비공개 글은 기존 비밀번호 길이 때문에 조회 불가능해지지 않는다.
4. 서비스 소유자가 웹 화면에서 최초 운영자 비밀번호를 직접 지정한다.
5. 로그인한 운영자가 현재 비밀번호 확인 후 새 비밀번호로 변경한다.
6. 비밀번호 원문이 Git, D1, 로그, URL, Telegram에 남지 않는다.
7. 비밀번호 변경 즉시 기존 운영자 세션을 모두 무효화한다.
8. 공개 화면의 모든 전화번호가 `010-3388-1597`로 일치한다.
9. 자동 알림 홍보 문구가 실제 알림 설정 상태와 모순되지 않는다.
10. 고객이 휴대전화 번호와 신청 비밀번호로 본인의 신청만 조회한다.
11. 홈페이지와 신청 조회 화면에 전체 고객 신청 목록을 노출하지 않는다.
12. 운영자가 접수 일련번호별 목록, 복합 검색, 업무·정산 상세 필드를 한 화면에서 관리한다.

### 4.2 비목표

- 고객 회원가입 및 고객 계정 비밀번호 변경
- 이메일·문자 기반 운영자 비밀번호 찾기
- 다수 운영자와 역할 기반 권한 관리
- 우편번호 자동 검색 API 도입
- Telegram 외 알림 채널 추가
- 기존 접수의 우편번호를 즉시 일괄 삭제하는 작업
- 공개 신청 게시판과 최근 신청 목록
- 신규 신청의 공개/비공개 선택
- 관리자 2단계 인증 구현

다수 운영자, 비밀번호 찾기, 2단계 인증은 후속 버전에서 별도로 설계한다.

### 4.3 성공 지표

| 지표 | 목표 |
|---|---:|
| 인증 전 고객 신청 목록·메타데이터 노출 | 0건 |
| 올바른 휴대전화+비밀번호 조회 성공률 | 99% 이상 |
| 잘못된 조합으로 타인 신청 노출 | 0건 |
| 개인 조회 응답 p95 | 2초 이내 목표 |
| 우편번호 없이 신규 신청 저장 성공률 | 99% 이상 |
| 운영자 최초 설정 및 비밀번호 변경 E2E | 100% 통과 |
| 공개 화면의 대표 연락처 불일치 | 0건 |

## 5. 사용자

### 5.1 고객

- 수리 신청을 빠르게 완료하려는 사용자
- 주소 체계나 우편번호를 정확히 알지 못할 수 있는 사용자
- 비공개 신청 내용을 간단한 비밀번호로 보호하려는 사용자
- 접수번호를 기억하지 못해도 휴대전화 번호와 비밀번호로 본인 신청을 찾으려는 사용자
- 모바일 환경에서 신청과 조회를 수행하는 사용자

### 5.2 운영자

- 컴박사 서비스의 단일 소유자
- 최초 배포 후 본인이 사용할 비밀번호를 직접 정하려는 사용자
- 개발자 도구 없이 관리자 화면에서 비밀번호를 변경하려는 사용자
- 이동 중 신규 접수를 빠르게 확인하려는 사용자

## 6. 핵심 제품 결정

| 항목 | 결정 |
|---|---|
| 우편번호 | 신규 신청 폼과 API에서 제거 |
| 이름·상세 주소 | 선택 입력, 이름 미입력 시 `미상` 저장 |
| 기존 우편번호 데이터 | 기존 보유기간 정책에 따라 유지 후 익명화하며 공개하지 않음 |
| 신규 신청 공개 범위 | 모두 `PRIVATE`, 공개/비공개 선택 UI 제거 |
| 전체 신청 게시판 | 폐지하고 홈의 최근 신청 목록도 제거 |
| 고객 조회 키 | 정규화한 휴대전화 번호 + 신청 비밀번호 |
| 조회 결과 | 두 값이 모두 일치하는 신청만 표시 |
| 조회 세션 | D1 기반 불투명 세션, 10분 만료 |
| 신규 비공개 비밀번호 | 4~20자 |
| 기존 비공개 글 조회 | 해당 기존 접수의 보유기간 종료까지 4~64자 입력 허용 |
| 비공개 비밀번호 저장 | 기존 Argon2id 해시 유지 |
| 운영자 비밀번호 저장 | D1 `admins.password_hash`에 Argon2id 해시 저장 |
| 최초 운영자 설정 | 일회성 `ADMIN_SETUP_TOKEN`으로 보호된 웹 설정 화면 |
| 운영자 비밀번호 변경 | 로그인 후 현재 비밀번호 재확인, 새 비밀번호 12~64자 |
| 비밀번호 변경 후 세션 | 모든 기존 운영자 세션 즉시 무효화 |
| 비밀번호 분실 복구 | MVP에서는 Cloudflare/D1 관리 절차로만 수행 |
| 대표 연락처 | `010-3388-1597` |
| 즉시 알림 카드 | 팝업이 아닌 상태·신뢰 표시 카드로 유지하되 실제 알림 설정에 따라 문구 분기 |
| 접수 일련번호 | 별도 AUTOINCREMENT 테이블로 고정 발급, 목록은 최신 번호순 |
| 관리자 목록 | 검색 폼 다음에 번호·접수구분·고객명·휴대폰·담당자·고객구분·상태·접수일 표시 |
| 관리자 상세 | 고객·장애·배정·일정·계산서·결제·정산·상태 필드 편집 및 이력 보존 |

## 7. 핵심 사용자 흐름

### 7.1 우편번호 없는 서비스 신청

1. 고객이 `/requests/new`에 진입한다.
2. 연락처와 기본 주소를 필수 입력하고 이름과 상세 주소는 필요한 경우에만 입력한다.
3. 우편번호 입력란은 표시되지 않는다.
4. 고객이 기기, 증상, 접수 내용을 입력한다.
5. 모든 신규 신청은 비공개이며 고객은 4~20자 조회 비밀번호를 필수로 입력한다.
6. 서버가 동일 규칙으로 검증하고 접수를 저장한다.
7. 새 레코드의 레거시 `postal_code` 필드에는 빈 문자열을 저장한다.
8. 접수번호를 발급하고 알림 outbox를 생성한다.
9. 이름이 비어 있으면 `미상`으로 저장하고 접수 관리대장용 고정 일련번호를 함께 발급한다.

### 7.2 내 신청 조회

1. 고객이 헤더나 홈의 `내 신청 조회`를 눌러 `/requests`에 진입한다.
2. 화면에는 전체 신청 목록 대신 휴대전화 번호와 신청 비밀번호 입력 폼이 표시된다.
3. 고객이 신청 시 사용한 휴대전화 번호와 비밀번호를 입력한다.
4. 서버는 전화번호를 숫자 10~11자리로 정규화하고 `REQUEST_LOOKUP_SECRET`으로 버전·길이 구분된 휴대전화+비밀번호 HMAC 조회 키를 계산한다.
5. 신규 접수는 조회 키와 정규화 전화번호가 모두 일치하는 후보를 찾은 뒤 각 후보의 Argon2id 해시로 비밀번호를 최종 확인한다.
6. 조회 키가 없는 기존 접수는 같은 전화번호의 후보를 조회하고 Argon2id 비밀번호를 검증한다.
7. 두 값이 모두 일치하는 신청만 조회 세션에 연결한다.
8. 성공 시 10분짜리 불투명 조회 세션 쿠키를 발급하고 일치한 신청 목록을 표시한다.
9. 여러 신청에 같은 비밀번호를 사용했다면 후보 상한 20건 안에서 일치하는 신청을 모두 표시하며, 다른 비밀번호의 신청은 표시하지 않는다.
10. 일치 결과가 없어도 전화번호 존재 여부를 알 수 없는 동일한 오류 문구를 표시한다.
11. 5회 연속 실패하면 클라이언트+전화번호 해시 기준으로 15분 동안 차단한다.

### 7.3 신청 상세 조회

1. 고객이 개인 조회 결과에서 접수를 선택한다.
2. 서버는 조회 세션이 해당 접수 ID에 대한 권한을 포함하는지 확인한다.
3. 권한이 있으면 접수 내용과 진행 상태를 표시한다.
4. 권한이 없거나 세션이 만료되면 `내 신청 조회` 화면으로 이동한다.
5. 기존 저장 URL을 위한 글별 비밀번호 해제 경로는 레거시 접수 보유기간 동안만 호환 경로로 유지한다.

### 7.4 최초 운영자 비밀번호 설정

1. 배포 관리자가 Cloudflare Secret에 강한 일회성 bootstrap secret `ADMIN_SETUP_TOKEN`을 등록한다.
2. 서비스 소유자가 `/admin/setup`에 접속한다.
3. 설정 토큰, 새 운영자 비밀번호, 비밀번호 확인을 입력한다.
4. 서버는 관리자 레코드가 아직 없는지 먼저 확인한다.
5. 서버는 설정 토큰을 상수 시간 비교하고 시도 횟수를 제한한다.
6. 고정 singleton 관리자 키로 조건부 INSERT를 실행해 동시 요청 중 한 건만 성공시킨다.
7. 운영자 비밀번호를 Argon2id로 해시해 D1에 저장한다.
8. 설정 성공 후 자동 로그인하지 않고 `/admin/login`으로 이동한다.
9. 관리자는 `ADMIN_SETUP_TOKEN`을 Cloudflare에서 삭제한다.
10. 관리자 레코드가 존재하면 `/admin/setup`은 항상 로그인 화면으로 이동한다.

### 7.5 운영자 비밀번호 변경

1. 로그인한 운영자가 `/admin/settings/security`에 진입한다.
2. 현재 비밀번호, 새 비밀번호, 새 비밀번호 확인을 입력한다.
3. 서버가 현재 비밀번호를 다시 검증한다.
4. 새 비밀번호가 12~64자이며 확인값과 같은지 검증한다.
5. 기존 비밀번호와 같은 값은 거부한다.
6. 새 Argon2id 해시 저장, `session_version` 증가, 감사 로그 기록을 하나의 D1 원자적 작업으로 실행한다.
7. 작업 중 하나라도 실패하면 전체 변경을 롤백하며 비밀번호 원문은 기록하지 않는다.
8. 현재 쿠키를 삭제하고 모든 기존 세션을 무효화한다.
9. 운영자는 새 비밀번호로 다시 로그인한다.

## 8. 기능 요구사항

### 8.1 대표 연락처

| ID | 요구사항 | 우선순위 | 인수 조건 |
|---|---|---:|---|
| FR-CONTACT-01 | 기본 대표 연락처를 `010-3388-1597`로 변경한다. | P0 | 헤더, 모바일 고정 버튼, 홈 CTA, 푸터, 개인정보 문의에 동일 번호가 표시됨 |
| FR-CONTACT-02 | 전화 링크는 숫자만 사용한 `tel:01033881597`로 생성한다. | P0 | 모바일에서 누르면 전화 앱이 올바른 번호로 열림 |
| FR-CONTACT-03 | 운영 배포에도 `NEXT_PUBLIC_BUSINESS_PHONE`을 동일 값으로 설정한다. | P0 | 코드 기본값과 런타임 값의 불일치가 없음 |
| FR-CONTACT-04 | 회귀 테스트에서 더미 번호 `010-0000-0000`의 공개 노출을 금지한다. | P0 | 빌드 결과 검색 시 더미 번호가 없음 |

### 8.2 우편번호 입력 제거

| ID | 요구사항 | 우선순위 | 인수 조건 |
|---|---|---:|---|
| FR-ADDR-01 | 서비스 신청 화면에서 우편번호 필드와 오류 영역을 제거한다. | P0 | 키보드 탭 순서와 모바일 레이아웃에 빈 공간이 남지 않음 |
| FR-ADDR-02 | 생성 API가 `postalCode` 누락을 오류로 처리하지 않는다. | P0 | 우편번호 없이 정상 신청이 생성됨 |
| FR-ADDR-03 | 새 접수는 레거시 `postal_code`에 빈 문자열을 저장한다. | P0 | 기존 D1 스키마와 배포 호환성을 유지함 |
| FR-ADDR-04 | 관리자 상세 주소에서 빈 대괄호 `[]`를 표시하지 않는다. | P0 | 신규 접수는 `기본 주소 상세 주소`만 표시됨 |
| FR-ADDR-05 | 개인정보 처리방침과 수집 동의 문구에서 우편번호를 제거한다. | P0 | 공개 문안이 실제 수집 항목과 일치함 |
| FR-ADDR-06 | 타입과 DTO에서 `postalCode`를 신규 생성 필수값으로 요구하지 않는다. | P0 | TypeScript 검사 통과 |
| FR-ADDR-07 | 기존 접수의 우편번호는 공개 화면·알림에 노출하지 않는다. | P0 | 기존 데이터 호환과 개인정보 최소 노출을 모두 만족함 |
| FR-ADDR-08 | 후속 스키마 정리 시 `postal_code` 삭제 여부를 별도 마이그레이션으로 결정한다. | P1 | 즉시 컬럼 삭제로 인한 기존 데이터 손실이 없음 |

### 8.3 신청 조회 비밀번호 4~20자

| ID | 요구사항 | 우선순위 | 인수 조건 |
|---|---|---:|---|
| FR-PRIVATE-01 | 신규 비공개 글 비밀번호는 4~20자로 제한한다. | P0 | 브라우저와 서버가 동일한 최소·최대 길이를 적용함 |
| FR-PRIVATE-02 | 공백을 포함한 입력을 임의로 잘라 저장하지 않는다. | P0 | 원문 기준 길이 검증 후 전체 값을 해시함 |
| FR-PRIVATE-03 | 비밀번호는 Argon2id와 고유 salt로 해시한다. | P0 | DB, 로그, 알림, 응답에 원문이 없음 |
| FR-PRIVATE-04 | 조회 API는 기존 글 호환을 위해 4~64자 입력을 허용한다. | P0 | 기존 21~64자 비밀번호 글도 계속 조회 가능 |
| FR-PRIVATE-05 | 신규 신청 화면에는 `4~20자` 안내를 표시한다. | P0 | 화면 문구와 서버 오류 문구가 일치함 |
| FR-PRIVATE-06 | 개인 조회와 레거시 글별 조회 모두 5회 실패 후 15분 차단한다. | P0 | 4자리 비밀번호의 대입 위험을 완화함 |
| FR-PRIVATE-07 | 레거시 글별 접근 쿠키는 글별 경로, HttpOnly, SameSite, 10분 만료를 유지한다. | P0 | 다른 글이나 장기 세션에 재사용할 수 없음 |
| FR-PRIVATE-08 | 조회 화면 입력란은 기존 글 호환을 위해 최대 64자를 허용한다. | P0 | 21~64자 기존 비밀번호가 UI에서 잘리거나 거부되지 않음 |
| FR-PRIVATE-09 | 기존 글의 4~64자 조회 호환은 해당 접수가 보유·조회 가능한 동안 유지한다. | P0 | 임의 날짜에 기존 비밀번호가 차단되지 않음 |
| FR-PRIVATE-10 | 비밀번호 길이는 Unicode code point 기준으로 계산하고 전부 공백인 값은 거부한다. | P0 | 한글·이모지 입력의 클라이언트/서버 길이 판정이 일치함 |

#### 보안 유의사항

4자리 비밀번호는 사용자 편의성은 높지만 추측 가능성이 커진다. 사용자의 명시적 요구를 반영해 허용하되 다음 완화책은 필수다.

- 서버 측 속도 제한을 브라우저 검증과 별개로 적용한다.
- 오류 응답에서 글 존재 여부와 비밀번호 오답을 구분해 노출하지 않는다.
- UI에는 `8자 이상 권장` 안내를 함께 제공한다.
- 고객 신청 조회 비밀번호와 운영자 비밀번호 정책을 혼용하지 않는다.

### 8.4 내 신청 조회

#### 제품 판단

현재처럼 신청량이 많지 않은 단계에서는 전체 신청 목록이 충분한 사회적 증거가 되기 어렵다. 오히려 빈 목록이 서비스 활성도가 낮다는 인상을 주고, 고객 신청 메타데이터를 공개할 이유만 늘어난다. 따라서 전체 게시판을 폐지하고 고객이 본인 정보로만 조회하는 구조를 채택한다.

| ID | 요구사항 | 우선순위 | 인수 조건 |
|---|---|---:|---|
| FR-LOOKUP-01 | `/requests`에서 전체 신청 목록과 공개 검색·필터를 제거한다. | P0 | 인증 전에는 어떤 신청 메타데이터도 목록으로 보이지 않음 |
| FR-LOOKUP-02 | 헤더 메뉴명을 `신청 현황`에서 `내 신청 조회`로 변경한다. | P0 | 데스크톱·모바일 메뉴가 동일함 |
| FR-LOOKUP-03 | 홈의 최근 신청 목록을 제거하고 `내 신청 조회` CTA와 개인정보 보호 안내로 대체한다. | P0 | 다른 고객의 이름·지역·증상이 홈에 표시되지 않음 |
| FR-LOOKUP-04 | 신규 신청의 공개/비공개 선택 UI를 제거하고 모두 `PRIVATE`로 저장한다. | P0 | 새 접수에 `PUBLIC` 값이 생성되지 않음 |
| FR-LOOKUP-05 | 모든 신규 신청에서 4~20자 조회 비밀번호를 필수로 받는다. | P0 | 비밀번호 없는 새 접수를 생성할 수 없음 |
| FR-LOOKUP-06 | HMAC 조회 키와 정규화 전화번호로 후보를 좁힌 뒤 Argon2id 해시까지 일치한 접수만 결과에 포함한다. | P0 | 전화번호만 같거나 비밀번호만 같은 접수는 노출되지 않음 |
| FR-LOOKUP-07 | 한 조회 요청에서 Argon2id 후보 검증은 최대 20건으로 제한한다. | P0 | 반복 검증을 이용한 자원 고갈을 제한함 |
| FR-LOOKUP-08 | 일치 결과가 없을 때 전화번호 존재 여부를 구분하지 않는 오류를 표시한다. | P0 | 모든 불일치가 `입력 정보를 확인해 주세요`로 응답함 |
| FR-LOOKUP-09 | 성공 시 10분 만료의 불투명 조회 세션을 D1에 생성한다. | P0 | 쿠키만 변조해 다른 신청을 추가할 수 없음 |
| FR-LOOKUP-10 | 조회 세션에는 일치한 내부 request ID만 연결하고 비밀번호 원문을 저장하지 않는다. | P0 | 세션 DB·쿠키·로그에 비밀번호가 없음 |
| FR-LOOKUP-11 | 조회 실패를 클라이언트+전화번호 해시 기준으로 제한한다. | P0 | 5회 실패 후 15분 차단 |
| FR-LOOKUP-12 | 직접 접수 상세 URL은 유효한 조회 세션 또는 레거시 글별 접근 세션을 요구한다. | P0 | URL 추측만으로 상세 내용을 볼 수 없음 |
| FR-LOOKUP-13 | 조회 화면과 결과 화면을 검색엔진에서 제외한다. | P0 | `noindex, nofollow`, 공용 캐시 금지 |
| FR-LOOKUP-14 | 조회 세션 만료·로그아웃 시 서버 세션을 폐기하고 쿠키를 삭제한다. | P0 | 만료된 세션으로 재조회 불가 |
| FR-LOOKUP-15 | 후보가 없어도 dummy Argon2id 검증을 수행하고 후보가 있으면 조기 성공 반환을 하지 않는다. | P0 | 응답 시간으로 전화번호 등록 여부를 쉽게 구분할 수 없음 |
| FR-LOOKUP-16 | 신규 접수에 HMAC-SHA-256 조회 키를 생성하고 인덱스로 후보를 검색한다. | P0 | 동일 전화번호의 무관한 해시를 반복 검증하지 않음 |
| FR-LOOKUP-17 | 조회 키 입력은 버전과 각 필드의 UTF-8 바이트 길이를 포함해 직렬화한다. | P0 | 전화번호·비밀번호 경계가 달라도 같은 HMAC 입력이 되지 않음 |
| FR-LOOKUP-18 | `REQUEST_LOOKUP_SECRET`이 없으면 신규 신청 저장과 개인 조회를 503으로 중단한다. | P0 | 조회 불가능한 신규 접수가 생성되지 않음 |
| FR-LOOKUP-19 | 배포 전 `lookup_key IS NULL`인 레거시 접수를 정규화 전화번호별로 집계하고, 한 번호에 20건을 초과하면 배포를 중단한다. | P0 | 올바른 레거시 비밀번호가 후보 상한 밖으로 밀려 조회되지 않는 일이 없음 |
| FR-LOOKUP-20 | `lookup_key`는 비고유 후보 인덱스로만 사용하고, 최종 인증 성공은 저장된 Argon2id 해시 검증으로 판정한다. | P0 | HMAC 값만 일치하거나 DB 값만 조작해서 조회 세션을 얻을 수 없음 |
| FR-LOOKUP-21 | 저장된 조회 Secret 확인값과 런타임 Secret이 다르면 신규 신청 저장과 개인 조회를 503으로 중단한다. | P0 | 잘못된 Secret으로 조회 불가능한 신규 키를 만들지 않음 |

조회 비밀번호는 고객 계정의 공통 비밀번호가 아니라 접수별 비밀번호다. 같은 휴대전화 번호로 여러 번 신청하면서 서로 다른 비밀번호를 사용했다면, 입력한 비밀번호와 일치하는 신청만 표시한다.

신규 접수는 `lookup_key + phone`으로 정확한 후보를 찾으므로 다른 비밀번호의 신청이 후보 상한을 차지하지 않는다. 같은 휴대전화 번호와 같은 비밀번호로 20건을 초과해 신청한 예외 상황에서는 최근 20건을 우선 표시하고 운영자에게 경고를 남긴다. 전체 이력 페이지네이션은 P1로 분리한다. 현재 운영 데이터가 적다는 전제는 배포 전 집계로 반드시 확인한다.

#### 기존 공개 접수 처리

기존 `PUBLIC` 접수는 자동 삭제하거나 임의 비밀번호를 부여하지 않는다.

1. 배포 전에 기존 `PUBLIC` 접수 수와 실제 고객 데이터 여부를 운영자가 확인한다.
2. 배포 즉시 공개 목록과 홈 노출을 중단한다.
3. 기존 공개 상세에서는 고객 개인정보와 본문을 더 이상 공개하지 않는다.
4. 실제 고객 접수는 운영자 화면에 유지하고, 고객 조회가 필요하면 운영자가 본인 확인 후 안내한다.
5. 테스트 데이터는 운영자 명시 승인 후 익명화하거나 삭제한다.
6. 비밀번호 해시가 있는 기존 `PRIVATE` 접수는 휴대전화+기존 비밀번호 조회 대상에 포함한다.

### 8.5 운영자 최초 설정과 로그인

| ID | 요구사항 | 우선순위 | 인수 조건 |
|---|---|---:|---|
| FR-ADMIN-01 | 운영자 비밀번호를 환경변수 평문 비교가 아닌 D1 Argon2id 해시로 검증한다. | P0 | `ADMIN_PASSWORD` 없이 로그인 가능 |
| FR-ADMIN-02 | 관리자 레코드가 없을 때만 `/admin/setup`을 허용한다. | P0 | 최초 설정 후 같은 URL로 재설정 불가 |
| FR-ADMIN-03 | 최초 설정은 Cloudflare Secret `ADMIN_SETUP_TOKEN`으로 보호한다. | P0 | 설정 토큰이 없거나 틀리면 관리자 생성 불가 |
| FR-ADMIN-04 | 설정 토큰을 URL 쿼리나 로그에 포함하지 않는다. | P0 | POST body로만 전송되고 서버 로그에 기록되지 않음 |
| FR-ADMIN-05 | 설정 토큰 검증 실패를 IP/클라이언트 기준으로 제한한다. | P0 | 5회 실패 후 15분 차단 |
| FR-ADMIN-06 | 운영자 비밀번호는 12~64자로 설정한다. | P0 | 서버와 브라우저 검증이 일치함 |
| FR-ADMIN-07 | 비밀번호와 확인값이 일치해야 생성한다. | P0 | 불일치 시 D1 변경 없음 |
| FR-ADMIN-08 | 최초 설정 성공 후 일반 로그인 화면에서 다시 로그인하게 한다. | P0 | 설정 토큰만으로 인증 세션을 얻지 못함 |
| FR-ADMIN-09 | 관리자 레코드가 없고 설정 토큰도 없으면 운영 UI를 잠근다. | P0 | 누구나 최초 관리자를 선점할 수 없음 |
| FR-ADMIN-10 | 기존 `ADMIN_PASSWORD` fallback 코드는 한 차례 안정화 배포 후 제거한다. | P1 | D1 관리자 생성 후 환경변수 비밀번호를 무시함 |
| FR-ADMIN-11 | 최초 관리자는 고정 singleton 키로 원자적으로 생성한다. | P0 | 동시에 두 설정 요청이 도착해도 관리자 행은 정확히 1개 |

#### 최초 설정에 일회성 토큰이 필요한 이유

비밀번호가 없는 상태에서 `/admin/setup`을 누구에게나 열면 최초 방문자가 관리자 권한을 선점할 수 있다. 따라서 “웹에서 원하는 비밀번호를 지정”하는 기능은 구현 가능하지만, 최초 소유자임을 증명하는 별도 신뢰 수단이 반드시 필요하다. MVP에서는 Cloudflare Secret의 bootstrap secret을 사용한다. 이 값은 서버가 자체 소비·회전시키는 토큰이 아니며, 관리자 생성 후 운영자가 Secret을 삭제해야 일회성이 완성된다.

### 8.6 운영자 비밀번호 웹 변경

| ID | 요구사항 | 우선순위 | 인수 조건 |
|---|---|---:|---|
| FR-ADMIN-PW-01 | 로그인한 운영자에게 보안 설정 화면을 제공한다. | P0 | 비로그인 사용자는 접근 불가 |
| FR-ADMIN-PW-02 | 변경 전 현재 비밀번호를 재확인한다. | P0 | 세션 탈취만으로 비밀번호 변경 불가 |
| FR-ADMIN-PW-03 | 새 비밀번호는 12~64자이며 확인값과 일치해야 한다. | P0 | 불일치·길이 오류 시 저장되지 않음 |
| FR-ADMIN-PW-04 | 기존 비밀번호와 같은 새 비밀번호를 거부한다. | P0 | 실제 변경이 없는 요청을 성공 처리하지 않음 |
| FR-ADMIN-PW-05 | 변경 성공 시 `session_version`을 증가시킨다. | P0 | 다른 기기의 기존 세션도 다음 요청부터 무효 |
| FR-ADMIN-PW-06 | 현재 브라우저 쿠키를 삭제하고 로그인 화면으로 이동한다. | P0 | 새 비밀번호로 재로그인해야 함 |
| FR-ADMIN-PW-07 | 성공·실패·차단 이벤트를 감사 로그에 기록한다. | P0 | 비밀번호와 설정 토큰 원문은 기록하지 않음 |
| FR-ADMIN-PW-08 | 변경 요청에 Same-Origin 검사와 속도 제한을 적용한다. | P0 | CSRF 및 반복 시도 방어 |
| FR-ADMIN-PW-09 | 해시·세션 버전·감사 로그 갱신을 하나의 D1 원자적 작업으로 처리한다. | P0 | 일부만 반영되는 상태가 발생하지 않음 |

#### 구현 가능성 결론

웹사이트에서 운영자 비밀번호를 변경하는 기능은 현재 기술 스택으로 구현 가능하다. D1에 관리자 해시와 세션 버전을 저장하고, 기존 Argon2id 모듈과 Cloudflare Secret을 재사용하면 된다.

단, “비밀번호를 잊은 상태에서 누구나 웹으로 재설정”하는 기능은 별도 본인 확인 채널이 없으므로 MVP에서 제공하지 않는다. 분실 시에는 Cloudflare 관리자 권한으로 일회성 복구 토큰을 등록하거나 D1 관리 명령을 수행한다.

### 8.7 `빠른 확인 · 접수 즉시 알림` 카드

#### 현재 목적

이 요소는 브라우저 팝업이나 모달이 아니다. 홈 화면 오른쪽 히어로 이미지 위에 항상 표시되는 정적 플로팅 카드다.

제품 목적은 다음과 같다.

1. 온라인 접수가 방치되지 않고 운영자에게 전달된다는 신뢰를 준다.
2. 전화하지 않아도 접수할 수 있다는 장점을 짧게 전달한다.
3. 히어로 영역에서 신청 CTA와 운영 처리 흐름을 시각적으로 연결한다.
4. 실제 구현된 Telegram 신규 접수 알림 기능을 사용자 가치로 설명한다.

#### 실제 기능과의 관계

- 신청이 D1에 저장되면 Telegram outbox가 생성된다.
- 서버는 저장 직후 Telegram 전송을 시도한다.
- Telegram 설정이 없거나 전송에 실패해도 고객 신청은 정상 저장된다.
- 현재 플로팅 카드는 outbox의 성공·실패 상태를 읽지 않는 정적 문구다.
- 따라서 Telegram이 설정되지 않은 환경에서도 `접수 즉시 알림`이 표시될 수 있다.

#### 제품 결정

카드 자체는 유지하되 실제 설정에 따라 문구를 분기한다.

| 조건 | 상단 문구 | 강조 문구 |
|---|---|---|
| Telegram 알림 사용 가능 | 운영자 알림 | 접수 후 자동 전송 |
| Telegram 알림 미설정 | 접수 확인 | 영업시간 내 연락 |

알림 상태 판정:

| 상태 | 조건 | 전송/재시도 |
|---|---|---|
| `DISABLED` | `TELEGRAM_NOTIFICATION_ENABLED=false` | 전송하지 않고 재시도하지 않음 |
| `CONFIG_REQUIRED` | 기능은 활성화했지만 bot token 또는 chat ID가 없음 | 설정 완료 전 재시도하지 않음 |
| `PENDING` | 필수 설정이 있고 전송 대기 중 | 즉시 또는 예약 재시도 |
| `SENT` | Telegram API 성공 | 완료 |
| `FAILED` | 잘못된 토큰, API 오류, 타임아웃 등 | 제한 횟수와 backoff에 따라 재시도 |

활성 카드 문구는 `TELEGRAM_NOTIFICATION_ENABLED=true`이고 두 필수 Secret이 모두 있을 때만 사용한다. 이 문구는 “자동 전송 시도”를 뜻하며 운영자가 즉시 상담하거나 Telegram 전달이 항상 성공한다는 보장이 아니다.

| ID | 요구사항 | 우선순위 | 인수 조건 |
|---|---|---:|---|
| FR-NOTICE-01 | 카드를 팝업처럼 포커스를 가로채거나 닫기 요구하는 UI로 만들지 않는다. | P0 | 키보드·스크린리더 흐름을 방해하지 않음 |
| FR-NOTICE-02 | 알림 사용 여부와 카드 문구를 서버 설정으로 연결한다. | P0 | 미설정 환경에서 자동 전송을 주장하지 않음 |
| FR-NOTICE-03 | `즉시 확인`처럼 운영 응답 시간을 보장하는 문구를 사용하지 않는다. | P0 | 자동 전송과 실제 상담 응답을 구분함 |
| FR-NOTICE-04 | 알림 실패가 고객 신청 실패로 처리되지 않게 한다. | P0 | Telegram 장애 중에도 접수번호 발급 |
| FR-NOTICE-05 | 운영자 화면에서 알림 상태와 재시도 기능을 유지한다. | P0 | `DISABLED`, `PENDING`, `SENT`, `FAILED`, `CONFIG_REQUIRED` 구분 |
| FR-NOTICE-06 | 알림 비활성 상태는 outbox에서 `DISABLED`로 기록하고 전송 대상에서 제외한다. | P0 | 의도적 비활성과 설정 누락을 구분함 |

## 9. 데이터 모델

### 9.1 `service_requests`

| 필드 | 변경 |
|---|---|
| `postal_code` | 레거시 필드로 유지, 신규 접수는 빈 문자열 저장 |
| `access_password_hash` | 기존 구조 유지, 신규 생성 정책만 4~20자로 변경 |
| `lookup_key` | HMAC-SHA-256(`v1 + phoneByteLength + phone + passwordByteLength + password`의 길이 구분 바이너리), 신규 접수 필수·기존 접수 nullable |
| `visibility` | 신규 접수는 항상 `PRIVATE`, 기존 값은 마이그레이션 검토용으로 유지 |
| `phone` | 숫자 10~11자리 정규화 유지, 개인 조회 후보 검색용 인덱스 추가 |
| 기타 필드 | 변경 없음 |

즉시 컬럼을 삭제하지 않는 이유는 Cloudflare D1 마이그레이션 위험을 줄이고 기존 접수 조회를 보존하기 위해서다. 기존 우편번호는 해당 서비스 종료 후 1년 보유기간이 끝나거나 익명화 요청이 처리될 때 삭제한다. 운영자가 월 1회 만료 접수 익명화 결과를 확인하며, 자동 만료 작업은 P1로 분리한다. 후속 버전에서 컬럼 제거 여부를 결정한다.

`lookup_key`는 비고유 컬럼이며 `lookup_key + phone` 복합 인덱스를 추가한다. 동일 전화번호와 동일 비밀번호로 여러 접수가 존재할 수 있으므로 UNIQUE 제약을 두지 않는다. 조회 시에는 두 컬럼이 모두 일치하는 행만 후보로 사용한다.

### 9.2 `customer_lookup_sessions` 신규 테이블

| 필드 | 형식 | 설명 |
|---|---|---|
| `id` | TEXT PK | 내부 세션 UUID |
| `token_hash` | TEXT UNIQUE NOT NULL | 쿠키 원문 토큰의 SHA-256 해시 |
| `expires_at` | TEXT NOT NULL | 생성 후 10분 |
| `created_at` | TEXT NOT NULL | 생성 시각 |

브라우저 쿠키에는 충분히 긴 무작위 원문 토큰만 저장하고, D1에는 해시만 저장한다.

### 9.3 `customer_lookup_session_requests` 신규 테이블

| 필드 | 형식 | 설명 |
|---|---|---|
| `session_id` | TEXT FK | 조회 세션 |
| `request_id` | TEXT FK | 비밀번호 검증에 성공한 접수 |
| `created_at` | TEXT NOT NULL | 연결 시각 |

`session_id + request_id`를 복합 기본 키로 사용한다. 상세 조회는 이 테이블에 연결된 접수만 허용한다.

### 9.4 `admins` 신규 테이블

| 필드 | 형식 | 설명 |
|---|---|---|
| `id` | TEXT PK | 고정값 `primary`, singleton 관리자 보장 |
| `login_name` | TEXT UNIQUE | 기본값 `admin`, 향후 다중 계정 대비 |
| `password_hash` | TEXT NOT NULL | Argon2id 인코딩 문자열 |
| `is_active` | INTEGER NOT NULL | `1`인 관리자만 로그인·세션 검증 허용 |
| `session_version` | INTEGER NOT NULL | 비밀번호 변경 시 증가 |
| `password_changed_at` | TEXT NOT NULL | 마지막 비밀번호 변경 시각 |
| `last_login_at` | TEXT nullable | 마지막 성공 로그인 |
| `created_at` | TEXT NOT NULL | 생성 시각 |
| `updated_at` | TEXT NOT NULL | 수정 시각 |

최초 생성은 `id='primary'` 조건부 INSERT로 수행한다. 기본 키 충돌 시 두 번째 요청은 실패하며, 존재 확인과 생성 결과를 같은 원자적 흐름에서 판정한다.

### 9.5 `admin_audit_logs` 신규 테이블

| 필드 | 형식 | 설명 |
|---|---|---|
| `id` | TEXT PK | 감사 로그 UUID |
| `admin_id` | TEXT nullable | 최초 설정 실패 등 계정 미확정 이벤트 허용 |
| `event_type` | TEXT NOT NULL | `SETUP_SUCCESS`, `LOGIN_SUCCESS`, `LOGIN_FAILED`, `PASSWORD_CHANGED` 등 |
| `client_hash` | TEXT nullable | 원본 IP가 아닌 비가역 해시 |
| `metadata` | TEXT nullable | 민감정보를 제외한 최소 JSON |
| `created_at` | TEXT NOT NULL | 이벤트 시각 |

감사 로그에는 비밀번호, 설정 토큰, 전체 IP, 세션 토큰을 저장하지 않는다.

### 9.6 `security_settings` 신규 테이블

| 필드 | 형식 | 설명 |
|---|---|---|
| `key` | TEXT PK | 설정 식별자 |
| `value` | TEXT NOT NULL | 비밀 원문이 아닌 검증값 |
| `updated_at` | TEXT NOT NULL | 갱신 시각 |

`request_lookup_secret_check`에는 `HMAC-SHA-256(REQUEST_LOOKUP_SECRET, "combaksa:lookup-secret-check:v1")` 결과를 저장한다. 최초 배포에서는 `lookup_key`가 채워진 접수가 하나도 없을 때만 이 확인값을 원자적으로 생성할 수 있다. 이후 런타임 Secret이 없거나 확인값과 다르면 개인 조회와 신규 신청을 fail-closed로 중단한다. 확인값은 Secret 원문을 대체하거나 복구하는 값이 아니다.

### 9.7 `access_attempts` 공유 속도 제한 테이블

| 필드 | 형식 | 설명 |
|---|---|---|
| `scope` | TEXT NOT NULL | `REQUEST_LOOKUP`, `ADMIN_LOGIN`, `ADMIN_SETUP`, `ADMIN_PASSWORD` 등 |
| `client_hash` | TEXT NOT NULL | 클라이언트 주소의 keyed HMAC |
| `phone_hash` | TEXT nullable | 개인 조회 시 정규화 전화번호의 keyed HMAC |
| `window_started_at` | TEXT NOT NULL | 제한 구간 시작 시각 |
| `failure_count` | INTEGER NOT NULL | 구간 내 실패 수 |
| `blocked_until` | TEXT nullable | 차단 종료 시각 |

`scope + client_hash + phone_hash + window_started_at`에 조회용 인덱스를 둔다. 원본 IP와 휴대전화 번호는 속도 제한 테이블에 저장하지 않는다.

## 10. 세션 설계

### 10.1 운영자 세션

기존 8시간 HMAC 쿠키 구조를 유지하되 payload를 다음과 같이 확장한다.

```text
adminId.expiry.sessionVersion.signature
```

검증 절차:

1. 서명과 만료 시각을 검증한다.
2. D1에서 활성 관리자와 현재 `session_version`을 조회한다.
3. 쿠키 버전과 DB 버전이 같을 때만 인증한다.
4. 비밀번호 변경 시 DB 버전을 증가시켜 모든 기존 쿠키를 무효화한다.

쿠키 요구사항:

- 이름: `combaksa_admin_session`
- `HttpOnly`
- `Secure`
- `SameSite=Strict`
- `Path=/`
- 최대 8시간
- 로그아웃·비밀번호 변경 시 즉시 만료
- 기존 `baroon_admin_session` 쿠키도 마이그레이션 시 삭제

### 10.2 고객 조회 세션

- 쿠키 이름: `combaksa_request_lookup`
- 브라우저에는 32바이트 이상의 무작위 토큰을 Base64URL로 저장한다.
- D1에는 토큰 원문이 아닌 SHA-256 해시를 저장한다.
- `HttpOnly`, `Secure`, `SameSite=Lax`, `Path=/requests`
- 절대 만료 10분
- 일치한 접수 ID만 연결 테이블에 저장한다.
- 비밀번호 원문과 휴대전화 번호를 세션 테이블에 저장하지 않는다.
- 상세 조회 때마다 토큰 해시, 만료, 연결된 request ID를 검증한다.
- 만료·로그아웃 시 D1 세션과 쿠키를 모두 폐기한다.
- 만료 세션은 조회 시 지연 삭제하고, P1에서 scheduled cleanup을 추가한다.

## 11. API 및 화면 변경

| Method | 경로 | 변경 내용 | 인증 |
|---|---|---|---|
| GET | `/requests/new` | 우편번호·공개 범위 제거, 조회 비밀번호 4~20자 안내 | 없음 |
| POST | `/api/requests` | 우편번호 검증 제거, 항상 비공개 저장, 비밀번호 4~20자 검증 | Same-Origin + 속도 제한 |
| GET | `/requests` | 휴대전화 번호+비밀번호 조회 폼 또는 인증된 결과 | 없음 또는 고객 조회 세션 |
| POST | `/api/requests/lookup` | 휴대전화 후보 조회와 Argon2id 검증, 조회 세션 생성 | Same-Origin + 속도 제한 |
| POST | `/requests/lookup/logout` | 고객 조회 세션·쿠키 폐기 | 고객 조회 세션 |
| GET | `/requests/:publicId` | 조회 세션에 연결된 신청 상세 | 고객 조회 세션 |
| POST | `/api/requests/:publicId/unlock` | 저장 URL 레거시 호환용 4~64자 글별 해제 | Same-Origin + 속도 제한 |
| GET | `/admin/setup` | 최초 운영자 설정 화면 | 관리자 미생성 상태만 |
| POST | `/api/admin/setup` | 설정 토큰 검증 후 관리자 생성 | 설정 토큰 + 속도 제한 |
| GET | `/admin/login` | DB 해시 기반 로그인 | 없음 |
| POST | `/api/admin/session` | DB 해시 검증, sessionVersion 포함 쿠키 발급 | Same-Origin + 속도 제한 |
| GET | `/admin/settings/security` | 운영자 보안 설정 | 관리자 |
| POST | `/api/admin/password` | 현재 비밀번호 재확인 후 변경 | 관리자 + Same-Origin + 속도 제한 |
| POST | `/api/admin/logout` | 현재 쿠키 만료 | 관리자 |

## 12. UI 문구

| 위치 | 변경 전 | 변경 후 |
|---|---|---|
| 상단·모바일 메뉴 | 신청 현황 | 내 신청 조회 |
| 홈 최근 신청 섹션 | 전체 고객 신청 목록 | 내 신청 조회 CTA + 개인정보 보호 안내 |
| 신청 폼 | 우편번호 * | 필드 삭제 |
| 신청 폼 공개 범위 | 공개/비공개 선택 | 선택 UI 삭제, 모든 신청 비공개 |
| 신규 조회 비밀번호 도움말 | 8~64자로 입력 | 4~20자로 입력해 주세요. 보안을 위해 8자 이상을 권장합니다. |
| 신규 조회 비밀번호 오류 | 비공개 비밀번호는 8~64자 | 신청 조회 비밀번호는 4~20자로 입력해 주세요. |
| 내 신청 조회 폼 | 공개 목록·검색 | 휴대전화 번호 + 신청 비밀번호 |
| 조회 폼 도움말 | 없음 | 신청할 때 사용한 휴대전화 번호와 해당 신청 비밀번호를 입력해 주세요. |
| 조회 불일치 오류 | 글 또는 비밀번호별 오류 | 입력 정보를 확인해 주세요. |
| 운영자 로그인 | 운영자 비밀번호로 로그인 | 설정한 운영자 비밀번호로 로그인 |
| 운영자 최초 설정 제목 | 없음 | 운영자 비밀번호 설정 |
| 운영자 변경 화면 제목 | 없음 | 운영자 비밀번호 변경 |
| 알림 활성 카드 | 빠른 확인 / 접수 즉시 알림 | 운영자 알림 / 접수 후 자동 전송 |
| 알림 비활성 카드 | 빠른 확인 / 접수 즉시 알림 | 접수 확인 / 영업시간 내 연락 |

## 13. 개인정보 및 보안 요구사항

1. 비밀번호는 Argon2id 해시로만 저장한다.
2. 현재 구현의 Argon2id 파라미터 `m=19456`, `t=2`, `p=1`을 최소 기준으로 유지한다.
3. 설정 토큰, 세션 서명 키, 조회·속도 제한 키와 Telegram 토큰은 Cloudflare Secret으로 관리한다.
4. 비밀값을 `wrangler.jsonc`, Git, 빌드 결과, Actions 로그에 기록하지 않는다.
5. `ADMIN_SESSION_SECRET`이 없으면 운영 환경 배포 또는 관리자 로그인을 실패 처리한다.
6. `ADMIN_SETUP_TOKEN`은 최초 설정 기간에만 존재해야 하며 관리자 생성 후 수동 삭제한다.
7. 관리자 설정·로그인·변경 요청에는 Same-Origin 검사와 속도 제한을 적용한다.
8. 관리자 비밀번호 변경 전 현재 비밀번호를 재확인한다.
9. 비밀번호 변경 후 서버 측 세션 버전을 갱신한다.
10. 우편번호를 개인정보 처리방침의 신규 수집 항목에서 제거한다.
11. 기존 우편번호는 보유기간 종료 또는 익명화 요청 시 다른 개인정보와 함께 삭제한다.
12. 공개 페이지, Telegram, 감사 로그에는 주소 원문과 비밀번호를 기록하지 않는다.
13. 설정·로그인·비밀번호 변경·비공개 조회의 속도 제한은 Worker 인스턴스 메모리가 아니라 D1 `access_attempts`처럼 모든 인스턴스가 공유하는 저장소에서 집행한다.
14. 신규 신청은 모두 비공개이며 인증 전 전체 신청 목록, 이름, 지역, 증상, 상태를 노출하지 않는다.
15. 휴대전화 번호+비밀번호 불일치는 전화번호 등록 여부를 드러내지 않는 동일 응답을 사용한다.
16. 조회 세션 쿠키 원문은 D1에 저장하지 않고 해시만 저장한다.
17. 고객 조회와 상세 응답에 `Cache-Control: private, no-store`를 적용한다.
18. 조회 페이지와 상세 페이지에 `noindex, nofollow`를 적용한다.
19. 기존 `PUBLIC` 접수는 배포 즉시 공개 목록과 공개 상세에서 제외한다.
20. 조회 키는 HMAC-SHA-256과 전용 `REQUEST_LOOKUP_SECRET`으로 생성하며 전화번호나 비밀번호 원문을 복원할 수 없게 한다.
21. `REQUEST_LOOKUP_SECRET`을 변경하면 기존 신규형 조회 키를 재생성할 수 없으므로 일반 배포 중 임의 회전하지 않는다.
22. `lookup_key`는 인증 결과가 아니라 후보 인덱스다. 최종 조회 세션 발급 전 Argon2id 검증을 반드시 수행한다.
23. 조회 키 직렬화는 버전, 필드 식별자, UTF-8 바이트 길이와 값을 순서대로 포함한다.
24. 속도 제한 식별자는 단순 SHA-256이 아닌 전용 Secret 기반 도메인 분리 HMAC을 사용한다.
25. `client_hash = HMAC-SHA-256(RATE_LIMIT_SECRET, encode("client-ip", clientAddress))`로 생성한다.
26. `phone_hash = HMAC-SHA-256(RATE_LIMIT_SECRET, encode("lookup-phone", normalizedPhone))`로 생성한다.
27. `RATE_LIMIT_SECRET`이 없으면 보호 대상 인증·조회 API를 503으로 중단한다.
28. 조회·속도 제한 Secret 원문은 D1, 로그, 응답, 소스맵에 기록하지 않는다.

참고 기준:

- [OWASP Password Storage Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html)
- [OWASP Authentication Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Authentication_Cheat_Sheet.html)
- [OWASP Session Management Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html)
- [Cloudflare Workers Secrets](https://developers.cloudflare.com/workers/configuration/secrets/)

## 14. 환경 설정

### 14.1 비밀이 아닌 런타임 설정

```text
NEXT_PUBLIC_BUSINESS_PHONE=010-3388-1597
TELEGRAM_NOTIFICATION_ENABLED=true|false
REQUEST_SUBMISSION_ENABLED=true|false
```

### 14.2 비밀 설정

```text
ADMIN_SESSION_SECRET=<충분히 긴 무작위 값>
ADMIN_SETUP_TOKEN=<최초 설정용 일회성 무작위 값>
TELEGRAM_BOT_TOKEN=<선택>
TELEGRAM_CHAT_ID=<선택>
REQUEST_ACCESS_SECRET=<레거시 글별 접근 쿠키 서명 값>
REQUEST_LOOKUP_SECRET=<휴대전화+비밀번호 조회 키용 무작위 값>
RATE_LIMIT_SECRET=<속도 제한 식별자 HMAC용 무작위 값>
```

`ADMIN_PASSWORD`는 D1 관리자 마이그레이션이 끝난 뒤 제거한다.

## 15. 코드 영향 범위

| 파일/영역 | 변경 |
|---|---|
| `components/site-header.tsx` | `신청 현황`을 `내 신청 조회`로 변경 |
| `components/request-form.tsx` | 우편번호·공개 범위 UI 제거, 조회 비밀번호 4~20자 필수 |
| `components/request-lookup-form.tsx` | 휴대전화 번호+비밀번호 조회 폼 신규 |
| `components/private-unlock.tsx` | 4~64자 조회 입력과 신규/레거시 정책 안내 |
| `lib/logic/request-service.ts` | 우편번호·공개 선택 제거, 생성 비밀번호 정책과 개인 조회 로직 |
| `lib/logic/customer-lookup.ts` | 후보 제한, 비밀번호 검증, 조회 세션 생성 |
| `lib/security/lookup-key.ts` | 휴대전화+비밀번호 HMAC 조회 키 생성 |
| `lib/security/request-guard.ts` | 속도 제한 식별자를 도메인 분리 HMAC으로 변경 |
| `app/api/requests/[publicId]/unlock/route.ts` | 조회 입력 4~64자 |
| `app/api/requests/lookup/route.ts` | 개인 조회 세션 생성 API |
| `app/requests/lookup/logout/route.ts` | `Path=/requests` 쿠키가 전달되는 개인 조회 세션 폐기 API |
| `app/requests/page.tsx` | 공개 게시판을 개인 조회 화면으로 교체 |
| `app/requests/[publicId]/page.tsx` | 개인 조회 세션 권한 검증 |
| `lib/domain.ts` | 신규 생성 DTO에서 우편번호 제거 |
| `app/admin/requests/[publicId]/page.tsx` | 우편번호 없는 주소 표시 |
| `app/privacy/page.tsx` | 수집 항목 문안 수정 |
| `lib/site-config.ts` | 대표 연락처 기본값 변경 |
| `wrangler.jsonc` | 공개 전화번호 설정 및 필요한 secret 선언 검토 |
| `lib/admin-auth.ts` | DB 해시 검증, sessionVersion 검증 |
| `data/request-repository.ts` | 공개 목록 경로 제거, 조회 키+전화번호 후보 조회, 알림 outbox 상태 저장 |
| `data/customer-lookup-repository.ts` | 조회 세션과 허용 접수 연결 저장 |
| `data/admin-repository.ts` | 관리자 생성·조회·비밀번호 변경·감사 로그 |
| `data/security-settings-repository.ts` | 조회 Secret 확인값 생성·검증 |
| `infrastructure/telegram.ts` | 활성·비활성·설정 누락·실패 상태 분기와 재시도 정책 |
| `app/admin/setup/page.tsx` | 최초 설정 화면 |
| `app/api/admin/setup/route.ts` | 최초 설정 API |
| `app/admin/settings/security/page.tsx` | 비밀번호 변경 화면 |
| `app/api/admin/password/route.ts` | 비밀번호 변경 API |
| `db/schema.ts`, `data/database.ts`, `drizzle/*` | nullable `lookup_key`, 비고유 `lookup_key+phone` 인덱스, 고객 조회 세션, 보안 설정, 공유 속도 제한, 관리자·감사 로그, outbox 상태 마이그레이션 |
| `app/page.tsx` | 최근 신청 목록 제거, 내 신청 CTA, 알림 카드 문구 조건 분기 |
| `tests/*` | 신규 정책 및 회귀 테스트 |

## 16. 마이그레이션과 배포 순서

### 단계 1: 사전 준비

1. `ADMIN_SESSION_SECRET`을 Cloudflare Secret으로 등록한다.
2. 강한 임시 `ADMIN_SETUP_TOKEN`을 Cloudflare Secret으로 등록한다.
3. 충분히 긴 무작위 `REQUEST_LOOKUP_SECRET`을 비밀번호 관리자에 보관한 뒤 Cloudflare Secret으로 등록한다.
4. 별도의 무작위 `RATE_LIMIT_SECRET`을 비밀번호 관리자에 보관한 뒤 Cloudflare Secret으로 등록한다.
5. 대표 연락처 공개 설정을 `010-3388-1597`로 확정한다.
6. Telegram 사용 여부와 실제 토큰 설정을 확인한다.
7. 운영 D1을 export해 복구 사본을 만든다.
8. 기존 `PUBLIC`·`PRIVATE` 접수 수와 실제 고객 데이터 여부를 운영자가 확인한다.
9. `lookup_key IS NULL`인 레거시 접수를 정규화 전화번호별로 집계한다. 한 번호가 20건을 초과하면 해당 고객 접수의 별도 전환·조회 계획을 승인하기 전까지 배포하지 않는다.
10. 테스트 데이터의 익명화·삭제는 운영자 승인 목록으로만 수행한다.

### 단계 2A: 개인정보 노출 차단 기준선 배포

1. 공개 신청 목록, 공개 검색·필터와 홈 최근 신청 목록을 먼저 제거한다.
2. 기존 공개 상세에서 고객 본문과 개인정보 노출을 중단한다.
3. 이 상태를 smoke test한 뒤 `privacy-baseline-v1` 태그로 고정한다.
4. 이후 모든 코드 롤백의 최저 기준은 이 태그이며, 그 이전 공개 게시판 버전으로는 되돌리지 않는다.

### 단계 2B: 데이터 및 기능 배포

1. `service_requests.lookup_key` nullable 컬럼과 비고유 `lookup_key + phone` 복합 인덱스를 명시적으로 추가한다.
2. `customer_lookup_sessions`, `customer_lookup_session_requests`, `security_settings`, `access_attempts`, `admins`, `admin_audit_logs` 테이블과 알림 outbox 상태 마이그레이션을 적용한다.
3. `lookup_key`가 채워진 행이 없는 것을 확인한 뒤 `request_lookup_secret_check`를 원자적으로 생성하고 런타임 Secret과 일치하는지 검증한다.
4. 우편번호 제거, 전체 비공개 신청, 휴대전화+비밀번호 조회, 연락처 변경을 함께 배포한다.
5. DB 기반 운영자 인증과 설정 화면을 배포한다.
6. 기존 환경변수 로그인은 관리자 레코드가 없을 때만 임시 마이그레이션 경로로 제한한다.
7. 모든 신규 접수는 `lookup_key`를 저장하며, 기존 행은 nullable 상태로 레거시 조회 경로를 사용한다.

### 단계 3: 운영자 최초 설정

1. 서비스 소유자가 `/admin/setup`에서 원하는 비밀번호를 설정한다.
2. `/admin/login`에서 새 비밀번호 로그인을 확인한다.
3. Cloudflare에서 `ADMIN_SETUP_TOKEN`을 삭제한다.
4. `ADMIN_PASSWORD`가 존재하면 운영 Secret을 삭제한다.
5. 환경변수 fallback 코드는 관리자 행이 존재할 때 비활성 상태로 한 차례 안정화 배포를 거친 뒤 P1에서 제거한다.

### 단계 4: 검증

1. 새 신청에 우편번호가 보이지 않는지 확인한다.
2. 새 신청에 공개 범위 선택이 없고 항상 비공개로 저장되는지 확인한다.
3. 4자와 20자 조회 비밀번호가 정상 생성되는지 확인한다.
4. 휴대전화 번호+비밀번호가 모두 일치한 신청만 목록에 표시되는지 확인한다.
5. 3자와 21자 신규 비밀번호가 거부되는지 확인한다.
6. 기존 21~64자 비밀번호 글을 개인 조회 또는 레거시 경로에서 조회할 수 있는지 확인한다.
7. 비밀번호 변경 후 다른 브라우저 세션이 만료되는지 확인한다.
8. 전체 게시판과 최근 신청 목록이 공개되지 않는지 확인한다.
9. 모든 공개 화면의 연락처를 확인한다.
10. Telegram 설정 상태에 맞는 카드 문구를 확인한다.
11. 고객 조회 로그아웃 요청에 `Path=/requests` 쿠키가 전달되고 D1 세션 행이 삭제되는지 확인한다.
12. `REQUEST_LOOKUP_SECRET` 누락·불일치 시 신규 신청과 개인 조회가 모두 503으로 닫히는지 확인한다.
13. `RATE_LIMIT_SECRET` 누락 시 보호 대상 인증·조회 API가 503으로 닫히는지 확인한다.

### 단계 5: 출시 승인과 복구

1. 스테이징 또는 별도 preview Worker+D1에서 E2E를 먼저 통과한다.
2. 운영 배포 후 서비스 소유자가 신청→개인 조회→관리자 처리 smoke test를 승인한다.
3. 전화번호만으로 타인의 접수가 보이거나 인증 전 목록이 노출되면 즉시 출시를 중단한다.
4. 코드 장애는 `privacy-baseline-v1` 또는 그 이후의 안정 커밋으로만 되돌린다. 공개 게시판·공개 상세 노출을 포함한 이전 버전은 롤백 대상으로 사용하지 않는다.
5. D1 복구 전 `REQUEST_SUBMISSION_ENABLED=false`로 설정해 신규 POST를 503으로 중단하고 조회 화면에는 점검 안내를 표시한다.
6. 장애 시점의 D1도 별도로 export한 뒤 사전 복구 사본을 restore한다.
7. 사전 사본 이후 생성·변경된 `service_requests`, 상태 이력, 알림 outbox를 장애 시점 export와 대조해 내부 ID 기준으로 재적용한다.
8. 복구 전후 총건수와 최근 접수번호를 운영자가 확인하고 승인한 뒤 `REQUEST_SUBMISSION_ENABLED=true`로 되돌린다.
9. `REQUEST_LOOKUP_SECRET`과 `RATE_LIMIT_SECRET`은 비밀번호 관리자에 보관한 같은 값을 복구 환경에 다시 주입한다.
10. 실패 로그와 복구 결과를 비밀값 없이 기록한다.

## 17. 테스트 요구사항

### 17.1 단위 테스트

- 신규 신청 비밀번호 3자 거부
- 신규 신청 비밀번호 4자 허용
- 신규 신청 비밀번호 20자 허용
- 신규 신청 비밀번호 21자 거부
- 우편번호가 없어도 신청 DTO 검증 성공
- 신규 신청의 `visibility=PRIVATE` 강제
- 휴대전화 번호 숫자 10~11자리 정규화
- 같은 휴대전화+비밀번호의 HMAC 조회 키 결정성
- 휴대전화 또는 비밀번호가 다르면 조회 키 불일치
- 길이 구분 직렬화로 `12`+`345`와 `123`+`45` 같은 필드 경계 충돌 방지
- 조회 후보에서 전화번호와 비밀번호가 모두 일치한 접수만 필터링
- 조회 키가 같아도 정규화 전화번호가 다르면 후보에서 제외
- 조회 키 일치 후 Argon2id 불일치 시 인증 실패
- 조회 세션 토큰 해시와 만료 검증
- 클라이언트 주소와 전화번호 속도 제한 키가 도메인 분리 HMAC으로 생성됨
- 운영자 비밀번호 Argon2id 해시·검증
- 설정 토큰 상수 시간 비교
- sessionVersion 불일치 세션 거부
- 전화 링크 숫자 정규화

### 17.2 통합 테스트

- 우편번호 없는 신청 저장 및 관리자 조회
- 기존 우편번호 데이터가 있는 접수 조회
- 신규 접수 모두 비공개 저장
- 신규 접수의 HMAC 조회 키 저장과 인덱스 조회
- `REQUEST_LOOKUP_SECRET` 누락·불일치 시 신규 접수와 개인 조회 503
- `RATE_LIMIT_SECRET` 누락 시 보호 대상 API 503
- 휴대전화+4~20자 비밀번호 개인 조회
- 같은 번호의 다른 비밀번호 접수 미노출
- 같은 번호와 같은 비밀번호의 복수 접수 조회
- 기존 21~64자 비밀번호 글 개인 조회·레거시 잠금 해제
- 인증 전 전체 접수 목록 미노출
- 조회 세션 만료·로그아웃·타인 접수 상세 거부
- `/requests/lookup/logout`에 조회 쿠키가 전달되고 D1 세션·연결 행이 삭제됨
- 레거시 정규화 전화번호별 20건 초과 사전 점검 시 출시 차단
- 최초 관리자 동시 생성 요청 중 한 건만 성공
- 잘못된 설정 토큰 반복 시 차단
- 운영자 로그인 성공·실패·차단
- 운영자 비밀번호 변경, 세션 버전, 감사 로그의 원자적 저장
- 비밀번호 변경 후 기존 세션 거부
- Telegram 비활성 중 접수 성공과 `DISABLED` 상태
- Telegram 미설정 중 접수 성공과 `CONFIG_REQUIRED` 상태
- Telegram 설정 오류·전송 실패 중 접수 성공과 `FAILED` 재시도 상태

### 17.3 E2E 테스트

1. 모바일 360px에서 우편번호 없는 신청 완료
2. 공개 범위 선택 없이 4자 비밀번호 신청 생성
3. 헤더 `내 신청 조회` → 휴대전화+비밀번호 → 본인 신청 목록
4. 잘못된 전화번호 또는 비밀번호로 결과 미노출
5. 홈과 `/requests`에서 전체 신청 목록 미노출
6. 운영자 최초 설정 → 로그인 → 대시보드 진입
7. 운영자 비밀번호 변경 → 자동 로그아웃 → 새 비밀번호 로그인
8. 이전 비밀번호 로그인 실패
9. 다른 브라우저의 기존 운영자 세션 만료
10. 헤더·홈·푸터의 전화 버튼 확인
11. 알림 활성/비활성 환경별 홈 카드 문구 확인
12. 고객 조회 로그아웃 후 뒤로 가기·직접 URL 접근 시 상세 재노출 없음
13. 점검 모드에서 신규 신청 POST가 503이고 기존 데이터가 변경되지 않음

### 17.4 보안 테스트

- 관리자 미생성 상태에서 설정 토큰 없이 계정 생성 불가
- 관리자 생성 후 `/admin/setup` 재사용 불가
- CSRF·다른 Origin 요청 거부
- 비밀번호와 설정 토큰이 응답·로그·소스맵에 없음
- HMAC 조회 키에서 휴대전화·비밀번호 원문이 노출되지 않음
- 쿠키 위조·만료·sessionVersion 불일치 거부
- 고객 조회 쿠키 원문이 D1에 저장되지 않는지 확인
- 전화번호 존재·비존재·비밀번호 오답의 응답 문구와 시간 차이 최소화
- 클라이언트+전화번호 해시 기준 5회 실패 후 15분 차단
- 속도 제한 저장소와 감사 로그에 원본 IP·휴대전화 번호가 없는지 확인
- 고객 조회·상세와 관리자 응답에 `Cache-Control: private, no-store`
- 인증 전 공개 HTML·RSC·API 응답에 신청 목록이나 상세 내용이 없음
- 조회 Secret 확인값 불일치가 fail-closed로 처리되고 새 `lookup_key`가 저장되지 않음
- `privacy-baseline-v1` 롤백 smoke test에서도 공개 목록·공개 상세가 다시 나타나지 않음

## 18. 인수 기준

다음 조건을 모두 충족해야 출시한다.

- 서비스 신청 화면에 우편번호 입력란이 없다.
- 우편번호 없이 실제 D1 접수가 저장된다.
- 개인정보 처리방침에 우편번호를 신규 필수 수집 항목으로 표시하지 않는다.
- 신규 비공개 비밀번호 4~20자 정책이 클라이언트와 서버에서 일치한다.
- 기존 긴 비밀번호 글의 조회가 유지된다.
- 신규 신청은 모두 비공개이며 공개 범위 선택이 없다.
- 홈과 `/requests`에 전체 신청 목록이 없다.
- 휴대전화 번호와 비밀번호가 모두 일치한 신청만 고객에게 표시된다.
- 조회 세션 만료·로그아웃·권한 없는 상세 접근이 차단된다.
- 레거시 전화번호별 후보가 20건을 넘으면 별도 처리 전 출시가 차단된다.
- 조회·속도 제한 Secret이 없거나 조회 Secret 확인값이 다르면 관련 기능이 503으로 안전하게 중단된다.
- 기존 공개 접수의 고객 본문이 인증 없이 노출되지 않는다.
- 운영자 비밀번호 원문이 소스·환경 공개 변수·D1·로그에 없다.
- 서비스 소유자가 보호된 웹 화면에서 최초 운영자 비밀번호를 지정할 수 있다.
- 로그인한 운영자가 웹에서 비밀번호를 변경할 수 있다.
- 비밀번호 변경 후 모든 기존 운영자 세션이 무효화된다.
- 공개 연락처가 모두 `010-3388-1597`이다.
- 알림 카드가 실제 설정과 일치하는 문구를 보여 준다.
- lint, 타입 검사, 프로덕션 빌드, 단위·통합·E2E 테스트가 통과한다.
- Cloudflare 배포 후 새 운영 URL에서 전체 흐름을 검수한다.
- 코드·D1 복구 훈련에서 공개 게시판 재노출과 복구 시점 이후 신규 접수 유실이 없다.

## 19. 위험과 완화책

| 위험 | 영향 | 완화 |
|---|---|---|
| 4자리 신청 비밀번호 대입 | 고객 신청 노출 위험 증가 | 5회/15분 차단, 8자 이상 권장, 10분 조회 세션 |
| 기존 긴 비밀번호 호환 중단 | 기존 고객이 글을 조회하지 못함 | 생성 정책과 조회 허용 길이를 분리 |
| 전화번호+비밀번호 대입 공격 | 다수 접수 탐색·자원 소모 | keyed HMAC 식별자 기반 공유 D1 속도 제한, 후보 20건 제한, 동일 오류 |
| 같은 번호로 다수 접수 | Argon2id 반복 검증 비용 증가 | 조회 키+번호 복합 인덱스, 레거시 20건 초과 출시 차단, 짧은 조회 세션 |
| 조회 키 Secret 분실·오설정·임의 회전 | 신규형 조회 키 검색 불가 | 별도 Secret 백업·확인값 검증·fail-closed, 계획 없는 회전 금지 |
| 고객 조회 로그아웃 시 쿠키 미전달 | 서버 세션이 남아 재접근 가능 | 쿠키 Path와 같은 `/requests` 하위 로그아웃 경로, D1 삭제 통합 테스트 |
| D1 사본 복구 중 신규 접수 유실 | 고객 신청·상태·알림 불일치 | 쓰기 점검 모드, 장애 시점 export, ID 기반 변경분 재적용과 운영자 건수 승인 |
| 코드 롤백으로 공개 게시판 재노출 | 고객 개인정보 재공개 | 개인정보 차단 기준선 태그보다 이전 버전으로 롤백 금지 |
| 기존 공개 접수 전환 | 비밀번호 없는 기존 고객 조회 단절 | 사전 목록 확인, 공개 노출 중단, 운영자 수동 안내 |
| 조회 세션 탈취 | 고객 신청 상세 노출 | 불투명 토큰, DB 해시 저장, 10분 만료, HttpOnly |
| 최초 설정 화면 선점 | 공격자가 운영자 권한 획득 | 일회성 Cloudflare Secret, 관리자 미생성 조건, 속도 제한 |
| 비밀번호 변경 후 탈취 세션 유지 | 공격자 접근 지속 | DB `session_version` 증가로 전 세션 만료 |
| 우편번호만 UI에서 제거 | 서버 검증 오류로 접수 실패 | UI·서버·DTO·관리자·개인정보 문안 동시 변경 |
| 알림 문구와 실제 상태 불일치 | 고객 신뢰 저하 | 알림 설정에 따른 문구 분기 |
| 런타임 전화번호 오버라이드 | 이전 번호가 계속 노출 | 코드 기본값과 배포 공개 설정 동시 변경 |
| 설정 토큰 장기 보관 | 최초 설정 경로 공격 표면 증가 | 설정 직후 Secret 삭제, 관리자 생성 후 경로 잠금 |

## 20. 구현 우선순위

### P0

- 대표 연락처 변경
- 우편번호 입력·검증·문안 제거
- 전체 신청 게시판과 최근 신청 목록 제거
- 신규 신청 전체 비공개화
- 휴대전화 번호+비밀번호 내 신청 조회
- D1 고객 조회 세션과 접근 제어
- 신규 신청 조회 비밀번호 4~20자
- 기존 긴 비밀번호 접수 조회 호환
- D1 기반 운영자 계정
- 보호된 최초 설정 화면
- 운영자 비밀번호 웹 변경
- 비밀번호 변경 후 세션 일괄 만료
- 알림 카드 문구 정합성
- 관련 테스트와 배포 검증

### P1

- 레거시 `postal_code` 컬럼 정리 검토
- 만료 고객 조회 세션 scheduled cleanup
- 레거시 글별 잠금 해제 경로 제거
- `ADMIN_PASSWORD` 호환 코드 완전 제거
- 비밀번호 분실용 오프라인 복구 명령
- 관리자 2단계 인증
- 알림 전송 상태 기반 운영 모니터링
- 동일 전화번호+비밀번호 접수 20건 초과 이력 페이지네이션
- 조회 Secret 버전 관리와 계획된 lazy rotation

## 21. 확정 사항과 추후 선택 사항

### 확정

- 서비스명: 컴박사
- 운영 URL: `combaksa-computer-repair.jung372.workers.dev`
- 대표 연락처: `010-3388-1597`
- 우편번호 신규 수집 제거
- 전체 신청 게시판 및 홈 최근 신청 목록 제거
- 신규 신청 전체 비공개
- 휴대전화 번호+비밀번호 기반 내 신청 조회
- 신규 비공개 글 비밀번호: 4~20자
- 운영자 비밀번호: 웹 최초 설정 및 로그인 후 변경
- 운영자 비밀번호 원문 저장 금지
- Telegram 미설정 시 플로팅 카드 문구: `접수 확인 / 영업시간 내 연락`
- 이름·상세 주소 선택 입력 및 이름 미입력 시 `미상`
- 관리자 접수 관리대장과 고정 일련번호
- 키워드·접수구분·담당자·고객구분·날짜·복수 처리상태 검색
- 고객명을 통한 관리자 접수 상세 편집 화면 진입

### 추후 선택

- Telegram 알림을 실제 운영에서 활성화할지
- 비밀번호 분실 복구를 Cloudflare 관리 절차로 둘지 별도 인증 채널을 추가할지
- 관리자 2단계 인증 도입 시점
- 기존 접수 우편번호의 조기 일괄 삭제 여부

## 22. 관리자 접수 관리대장 요구사항

### 22.1 목록과 검색

| ID | 요구사항 | 인수 조건 |
|---|---|---|
| FR-OPS-01 | 각 접수에 삭제·필터와 관계없이 유지되는 정수 일련번호를 발급한다. | 기존 접수는 생성일 오름차순으로 보정하고 신규 접수는 자동 증가함 |
| FR-OPS-02 | 관리자 상단에 검색어, 접수구분, 담당기사, 고객구분 필터를 표시한다. | 검색 폼이 접수 목록보다 먼저 표시됨 |
| FR-OPS-03 | 통합 날짜, 접수일, 완료일 범위 검색을 제공한다. | 통합 날짜는 접수일 또는 완료일이 범위에 포함된 행을 반환함 |
| FR-OPS-04 | 요청된 12개 처리상태를 복수 선택하고 모두선택할 수 있다. | 선택된 상태 중 하나와 일치하는 행만 반환함 |
| FR-OPS-05 | 목록 컬럼은 번호, 접수구분, 고객명, 휴대폰, 담당자, 고객구분, 처리상태, 접수일 순서로 표시한다. | 최신 일련번호순 최대 200건을 표시함 |
| FR-OPS-06 | 휴대폰과 담당자 연락처에 모바일 SMS 링크를 제공한다. | 번호가 있는 경우 `sms:` 링크가 열림 |

### 22.2 상세 접수 관리

| ID | 요구사항 | 인수 조건 |
|---|---|---|
| FR-OPS-07 | 고객명을 누르면 외부 사이트가 아닌 내부 관리자 상세 화면으로 이동한다. | 해당 일련번호와 공개 접수 ID가 함께 확인됨 |
| FR-OPS-08 | 고객명, 일반전화, 주소, 계산서, 제목, 장애현상, 관리자메모를 편집한다. | 저장 후 다시 열어도 값이 유지됨 |
| FR-OPS-09 | 접수구분, 담당자, 담당자 연락처, 고객접수구분, 고객분류와 주요 날짜를 편집한다. | 목록의 담당자·구분·접수일에 즉시 반영됨 |
| FR-OPS-10 | 결제방법, 총수금액, 자재비, 부가세, 기사수익, 사무실입금액을 원 단위 정수로 저장한다. | 음수·비정수·허용 범위 초과 입력을 서버가 거부함 |
| FR-OPS-11 | 부가세와 사무실입금액 계산 보조 기능을 제공한다. | 총수금액·자재비·기사수익을 기준으로 폼 값을 계산함 |
| FR-OPS-12 | 상태 변경 시 고객 공개 메모와 변경 이력을 남긴다. | 관리자·시각·상태·공개 메모가 이력에 추가됨 |
| FR-OPS-13 | 고객 조회 키 보호를 위해 관리자 상세의 휴대전화는 읽기 전용으로 표시한다. | 비밀번호를 모르는 상태에서 번호만 바꿔 고객 조회가 깨지지 않음 |

## 23. 참고 자료

- [OWASP Password Storage Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html)
- [OWASP Authentication Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Authentication_Cheat_Sheet.html)
- [OWASP Session Management Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html)
- [OWASP Forgot Password Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Forgot_Password_Cheat_Sheet.html)
- [Cloudflare Workers Secrets](https://developers.cloudflare.com/workers/configuration/secrets/)
- [Telegram Bot API](https://core.telegram.org/bots/api)
